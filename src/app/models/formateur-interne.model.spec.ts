import { FormateurInterne } from './formateur-interne.model';

describe('FormateurInterne', () => {
  it('should create an instance', () => {
    expect(new FormateurInterne()).toBeTruthy();
  });
});
